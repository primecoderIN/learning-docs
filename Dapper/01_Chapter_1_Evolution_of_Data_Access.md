# Chapter 1: The Evolution of Data Access in .NET

## 1. Learning Objectives

By the end of this chapter, you will be able to:
*   Understand the historical context of data access in the .NET ecosystem and the precise engineering challenges that led Stack Overflow to build Dapper.
*   Differentiate deeply between Full ORMs (Entity Framework Core, NHibernate) and Micro ORMs (Dapper) at the architectural and IL (Intermediate Language) levels.
*   Identify real-world, enterprise-scale architectural scenarios where Dapper is the optimal choice for SaaS platforms.
*   Analyze the trade-offs of using a Micro ORM within a Clean Architecture and CQRS (Command Query Responsibility Segregation) setup.
*   Conceptualize the foundational domain for the Multi-Tenant EV Charging Management Platform case study.

## 2. Introduction

In the domain of enterprise software engineering, data access is rarely just an implementation detail—it is the central pillar around which application performance, scalability, and operational maintainability revolve. For decades, .NET engineers have oscillated between the raw, unbridled performance of ADO.NET and the developer-friendly, heavily abstracted world of Full Object-Relational Mappers (ORMs) like Entity Framework (EF) and NHibernate.

As applications transition from internal corporate monoliths to globally distributed, multi-tenant SaaS platforms, a chasm emerges. Abstractions that accelerate development in the early stages (like Change Tracking and Lazy Loading) often become the very bottlenecks that cripple an application under peak load. This chapter explores the origins of Dapper, a tool built out of absolute engineering necessity to bridge this chasm, offering the performance of raw ADO.NET with a developer experience that rivals modern ORMs.

### The History of Dapper
Before Dapper's inception in 2011, .NET developers faced a stark, binary choice. Option A was **ADO.NET**: writing imperative, boilerplate-heavy code utilizing `SqlConnection`, `SqlCommand`, and `SqlDataReader`. This approach yielded maximum performance because it minimized allocations and abstraction layers. However, it was incredibly verbose, error-prone, and tedious. Mapping a result set to a strongly typed C# object required manually checking for `DBNull`, managing column ordinals, and writing custom mapping logic for every single entity.

Option B was a **Full ORM** (like NHibernate or early versions of Entity Framework). These frameworks provided massive productivity boosts through LINQ-to-SQL translations, Change Tracking, and Unit of Work patterns. However, they carried heavy runtime overhead: memory allocations for change trackers, complex state management graphs, and the notorious "N+1 query" problem caused by unoptimized lazy loading. Furthermore, the T-SQL generated by Full ORMs was often suboptimal for complex enterprise queries involving window functions, CTEs (Common Table Expressions), or pivot tables.

### Why Stack Overflow Built Dapper
In 2011, Stack Overflow was facing unprecedented scaling challenges. Serving thousands of page views per second, their telemetry indicated that LINQ-to-SQL (their initial ORM choice) was generating overly complex queries and, more importantly, consuming massive amounts of CPU and memory for object mapping and state management. Falling back to writing raw ADO.NET mapping logic was unfeasible given the massive size of their rapidly iterating codebase.

Engineers Marc Gravell and Sam Saffron needed a solution that added zero overhead to ADO.NET while completely eliminating the boilerplate of object mapping. They built **Dapper**: a single-file Micro ORM (initially just `SqlMapper.cs` dropped directly into their project) that used Reflection and Intermediate Language (IL) generation at runtime to create ultra-fast, cached object mappers.

The result? Dapper executed queries and mapped results at speeds nearly identical to hand-tuned ADO.NET, while reducing thousands of lines of boilerplate code to a simple extension method: `connection.Query<T>(sql)`.

### Micro ORM vs Full ORM
Understanding the fundamental architectural distinction between these two paradigms is crucial for any Solution Architect. Dapper is not a "lesser" EF Core; it is a fundamentally different tool designed to solve a different problem.

| Feature | Full ORM (EF Core) | Micro ORM (Dapper) |
| :--- | :--- | :--- |
| **SQL Generation** | Translates LINQ expressions into T-SQL at runtime. | None. You write the raw T-SQL. |
| **Change Tracking** | Tracks entity state (Added, Modified, Deleted) in memory. | None. Completely stateless execution. |
| **Object Mapping** | Complex mapping, relationships, value converters. | Fast, direct mapping via dynamic IL Emit. |
| **Lazy Loading** | Supported (though discouraged in modern architectures). | Not supported by design. |
| **Unit of Work** | Built-in via `DbContext.SaveChanges()`. | Manual (via `SqlTransaction` and custom UoW). |
| **Performance** | Good (with extensive tuning and AsNoTracking). | Exceptional (virtually identical to raw ADO.NET). |
| **Primary Use Case** | CRUD heavy operations, complex domain logic enforcement. | High-volume reads, complex custom SQL, reporting. |

### Real-World Use Cases
For a Principal Engineer, technology selection is about matching the tool to the architectural context. Dapper excels in:
1. **CQRS Read Models:** In a Command Query Responsibility Segregation (CQRS) architecture, the read side requires blazing fast execution and flattened DTOs. Dapper is the undisputed king of the read model.
2. **High-Volume SaaS Platforms:** Applications serving multi-tenant traffic cannot afford the memory overhead of change trackers on every HTTP request.
3. **Legacy Database Integration:** When working with databases containing hundreds of stored procedures, complex views, or denormalized tables, hand-writing SQL and mapping it with Dapper is far safer than attempting to coerce EF Core into understanding a legacy schema.
4. **Microservices:** Lightweight microservices that require rapid startup times and minimal memory footprints benefit immensely from Dapper's lack of heavy model bootstrapping (no `OnModelCreating` overhead).

## 3. Core Concepts

At a conceptual level, Dapper operates as an intelligent extension to the ADO.NET `IDbConnection` interface. 

### How it works internally (High Level)
When you instruct Dapper to execute a query, it intercepts the call before it hits the database.
1. It analyzes the SQL string and the types of the parameters you passed.
2. It generates a unique hash for this specific query and type combination.
3. It checks a highly optimized internal `ConcurrentDictionary` (the cache).
4. If it's the first time running this query, Dapper uses `System.Reflection.Emit` to write a custom, highly optimized method in raw IL (Intermediate Language) that knows exactly how to map the resulting `SqlDataReader` to your C# object.
5. It compiles this IL, stores it in the cache, and executes it.
6. On all subsequent calls, it skips reflection entirely and uses the cached IL delegate, resulting in near-zero mapping overhead.

## 4. Visual Diagrams

```text
=============================================================================
             ARCHITECTURAL COMPARISON: EF CORE VS DAPPER IN CQRS
=============================================================================

                   [ Web API / Minimal API Endpoints ]
                                   │
                                   ▼
                            [ MediatR Bus ]
                                   │
         ┌─────────────────────────┴─────────────────────────┐
         │                                                   │
  [ COMMAND PIPELINE (Writes) ]                       [ QUERY PIPELINE (Reads) ]
         │                                                   │
  [ Command Handler ]                                 [ Query Handler ]
         │                                                   │
  [ Entity Framework Core ]                           [ Dapper ]
  - Translates LINQ to SQL                            - Takes raw T-SQL
  - Tracks Changes in Memory                          - Emits IL for fast mapping
  - Enforces Domain Invariants                        - Maps directly to flat DTOs
  - Manages Unit of Work                              - Stateless & allocation-free
         │                                                   │
         ▼                                                   ▼
  [ Microsoft.Data.SqlClient ]                        [ Microsoft.Data.SqlClient ]
         │                                                   │
         └─────────────────────────┬─────────────────────────┘
                                   ▼
                             [ SQL Server ]
                             [ Storage Engine ]
```

## 5. Enterprise SaaS Perspective

Why do multi-tenant SaaS platforms gravitate towards Dapper? The answer lies in **Tenant Isolation** and **Memory Density**.

In a multi-tenant environment, you might be serving hundreds of tenants from a single database (using Row-Level Security or TenantId columns) or mapping tenants to separate databases. 
When executing a query for Tenant A, you do not want the framework caching execution plans or state that might inadvertently leak or slow down Tenant B. Dapper's stateless nature means that as soon as the query executes, the memory is ready to be garbage collected (if mapped to structs or small DTOs). There is no lingering `DbContext` holding onto memory.

Furthermore, SaaS applications live and die by their API response times. A 50ms reduction in API latency across 10,000 requests per minute saves significant compute resources, allowing organizations to scale out with fewer Azure App Service instances or Kubernetes pods. Dapper provides this mechanical sympathy out of the box.

## 6. Real Production Case Study

Throughout this book, we will contextualize every Dapper feature by building a **Multi-Tenant EV Charging Management Platform**.

### The Domain
Electric Vehicle charging infrastructure is inherently complex. Our platform must manage:
*   **Tenants:** The root charging network operators (e.g., "ChargePoint", "Electrify America").
*   **Organizations:** Sub-operators or site hosts (e.g., a supermarket chain that hosts chargers).
*   **Sites:** The physical locations of the chargers.
*   **Chargers (EVSEs):** The physical hardware.
*   **Charging Sessions:** High-volume transactional data recording power drawn (kWh) over time, telemetry, and metering.
*   **Transactions/Invoices:** Financial data representing the cost of a session.

### The Challenge
A single EV charger might send a "Heartbeat" or "MeterValue" payload every 15 seconds. If a Tenant operates 10,000 chargers, the backend must process ~40,000 requests per minute just for telemetry. We cannot afford to instantiate an EF Core `DbContext`, track entities, and translate LINQ for these high-throughput endpoints. We need the raw speed of Dapper to quickly execute `UPSERT` statements and rapidly read configuration data from SQL Server.

## 7. Common Mistakes

### Beginner
*   **Mistake:** Assuming Dapper will generate SQL for you (e.g., trying to call `connection.Insert(user)` without writing the SQL or using an extension library like Dapper.Contrib).
*   **Correction:** Dapper is purely an object mapper. You are fundamentally responsible for writing optimized T-SQL.
*   **Mistake:** Using string interpolation for SQL queries (e.g., `$"SELECT * FROM Users WHERE Id = {id}"`).
*   **Correction:** This causes severe SQL injection vulnerabilities. Always use parameterized queries (e.g., `SELECT * FROM Users WHERE Id = @Id`).

### Intermediate
*   **Mistake:** Not understanding the difference between Buffered and Unbuffered queries, leading to `OutOfMemoryException` when querying millions of rows using the default `Query<T>` behavior.
*   **Correction:** Use `buffered: false` for large datasets to stream the results directly from the `SqlDataReader`.
*   **Mistake:** Using `Query<T>` and then immediately calling `.FirstOrDefault()` via LINQ (e.g., `connection.Query<User>(sql).FirstOrDefault()`).
*   **Correction:** This allocates a `List<T>`, populates it with the entire result set, and then discards all but the first item. Use `connection.QueryFirstOrDefault<T>(sql)` directly.

### Senior
*   **Mistake:** Wrapping every Dapper call in a generic Repository pattern that perfectly mimics EF Core (e.g., trying to build a generic `Repository<T>` with generic `Get`, `Add`, `Update`, `Delete` methods via Reflection).
*   **Correction:** Dapper shines with specific, purpose-built, optimized SQL queries. Over-abstracting Dapper defeats its purpose and limits your ability to optimize specific T-SQL statements (like using SQL Server `MERGE` or specialized index hints). Use specific repositories (e.g., `IInvoiceRepository`) or CQRS Query Handlers.

### Architect
*   **Mistake:** Forcing a binary choice between EF Core and Dapper across the entire enterprise, instigating framework wars within the engineering team.
*   **Correction:** The most resilient modern SaaS architectures use a polyglot persistence approach: EF Core for the Command (Write) stack where Change Tracking and complex domain graph validations are beneficial, and Dapper for the Query (Read) stack where raw performance, DTO projection, and specialized SQL are paramount.

## 8. Interview Questions

### Beginner (10)
1.  **What exactly is Dapper?**
    *Answer:* Dapper is an open-source object-relational mapping (ORM) product for the .NET platform. It is classified as a "Micro ORM" because it focuses strictly on mapping database results to C# objects and does not handle SQL generation or change tracking.
2.  **How do you install Dapper into a .NET project?**
    *Answer:* Via the NuGet package manager, typically by running `dotnet add package Dapper` in the CLI.
3.  **What interface does Dapper extend to provide its functionality?**
    *Answer:* It extends the `IDbConnection` interface (from `System.Data`).
4.  **How does Dapper protect against SQL Injection?**
    *Answer:* By enforcing the use of parameterized queries. You pass an anonymous object or `DynamicParameters` to Dapper, which translates them into proper `SqlParameter` objects at the ADO.NET level.
5.  **What is the difference between `Query<T>` and `Execute`?**
    *Answer:* `Query<T>` is used for statements that return tabular data (like SELECT) and maps the result to type `T`. `Execute` is used for commands that do not return data (like INSERT, UPDATE, DELETE) and returns the number of rows affected.
6.  **Can Dapper be used with databases other than SQL Server?**
    *Answer:* Yes. Because it operates on `IDbConnection`, it works with any database that provides an ADO.NET provider, including PostgreSQL (Npgsql), MySQL (MySql.Data), and SQLite (Microsoft.Data.Sqlite).
7.  **Do I need to mark my C# properties with attributes for Dapper to map them?**
    *Answer:* Generally, no. Dapper automatically maps database column names to C# property names ignoring case. You only need custom logic if the names completely differ.
8.  **Does Dapper support asynchronous execution?**
    *Answer:* Yes, Dapper provides `*Async` equivalents for almost all its methods, such as `QueryAsync<T>` and `ExecuteAsync`.
9.  **What happens if a database column is NULL but my C# property is a non-nullable integer (`int`)?**
    *Answer:* Dapper will throw a `DataException` indicating a parsing error. You must use nullable types (e.g., `int?`) in C# to handle database NULLs.
10. **What namespace must be imported to use Dapper methods?**
    *Answer:* `using Dapper;`

### Intermediate (10)
11. **Explain the mechanical difference between a Full ORM like Entity Framework Core and Dapper.**
    *Answer:* A Full ORM abstracts the database entirely, tracking object state (Added, Modified) in memory and dynamically generating SQL from code (LINQ). Dapper executes raw SQL provided by the developer and focuses solely on fast object mapping using emitted IL. Dapper has no concept of an object's state relative to the database.
12. **Why shouldn't you use `string.Format` or string interpolation to build SQL queries in Dapper?**
    *Answer:* It completely bypasses Dapper's parameterization, exposing the application to SQL Injection attacks, and ruins SQL Server's execution plan cache because every query string looks unique to the database engine.
13. **What is an "Unbuffered" query in Dapper and when would you use it?**
    *Answer:* By default, Dapper buffers results, loading the entire `SqlDataReader` into a `List<T>` in memory before returning. By passing `buffered: false`, Dapper yields results one by one directly from the reader. This is crucial when processing massive datasets to prevent `OutOfMemoryException`.
14. **How does Dapper handle mapping multiple tables (JOINs) into a single C# object graph?**
    *Answer:* Dapper provides a `Query<TFirst, TSecond, TReturn>` overload (often called Multi-Mapping) where you provide a mapping function to stitch the objects together based on a `splitOn` parameter (usually the ID of the joined table).
15. **What is `DynamicParameters` and why use it over an anonymous object?**
    *Answer:* `DynamicParameters` allows you to dynamically build a parameter list at runtime (e.g., looping through a list to add parameters) and is required when you need to specify `ParameterDirection.Output` or `ParameterDirection.ReturnValue` for stored procedures.
16. **How does Dapper map snake_case database columns to PascalCase C# properties?**
    *Answer:* Out of the box, Dapper ignores underscores. A column named `first_name` will automatically map to a C# property named `FirstName`.
17. **Can Dapper map to immutable C# records or classes with private setters?**
    *Answer:* Yes, Dapper can map to constructors. If the column names match the constructor parameter names (case-insensitive), Dapper will invoke the parameterized constructor to create immutable objects.
18. **What is the `CommandDefinition` struct?**
    *Answer:* It's a struct used to pass execution details to Dapper, allowing you to specify the SQL, parameters, command timeout, transaction, command type, and cancellation tokens in a strongly-typed manner.
19. **How do you execute a Stored Procedure using Dapper?**
    *Answer:* You pass the stored procedure name as the SQL string, and set `commandType: CommandType.StoredProcedure` in the method call or via a `CommandDefinition`.
20. **Why might `QueryFirst` be dangerous if you aren't sure a record exists?**
    *Answer:* `QueryFirst` will throw an `InvalidOperationException` if the result set is empty. You should use `QueryFirstOrDefault`, which returns the type's default value (usually `null`) if no rows are found.

### Senior (10)
21. **Under the hood, how does Dapper achieve its performance and avoid the reflection penalty on every query?**
    *Answer:* Dapper uses `System.Reflection.Emit` (specifically `DynamicMethod` and `ILGenerator`) to dynamically write raw IL instructions at runtime. It inspects the `IDataReader` and the target type once, emits highly optimized IL to map them, compiles it to a delegate, and caches it in a `ConcurrentDictionary` keyed by the query hash. Subsequent executions use the cached delegate, bypassing reflection entirely.
22. **Explain the potential memory leak issue if you dynamically generate SQL strings (e.g., appending IN clauses manually) instead of using parameters.**
    *Answer:* Dapper caches its IL delegates in a static `ConcurrentDictionary` based on the SQL string. If you generate dynamic SQL strings where the string is unique every time (e.g., `SELECT * FROM Users WHERE Id IN (1, 2, 3)` vs `(4, 5, 6)`), Dapper will generate and compile a new IL delegate for every query and store it in the cache indefinitely, eventually causing a severe memory leak. You must use Dapper's native list parameterization (e.g., `WHERE Id IN @Ids`).
23. **How do you handle complex, nested object hierarchies that require mapping from multiple flat SQL queries without N+1 issues?**
    *Answer:* You can use Dapper's `QueryMultiple` (GridReader) to execute multiple SELECT statements in a single database round-trip. You then read each result set into memory and use C# LINQ (e.g., `GroupBy`, `ToDictionary`) to manually stitch the hierarchical graph together in the application layer, ensuring only one network call is made.
24. **In a CQRS architecture, how do you manage Transactions if a Command requires both Dapper and EF Core?**
    *Answer:* You must share the underlying `DbConnection` and `DbTransaction`. You can open a connection, begin a `SqlTransaction`, assign the connection and transaction to EF Core's `DbContext.Database.UseTransaction()`, and pass the exact same `SqlTransaction` object into Dapper's extension methods.
25. **What is the `SqlMapper.TypeHandler<T>` and when is it necessary?**
    *Answer:* A custom `TypeHandler` instructs Dapper on how to parse a complex C# type (like a custom Value Object, `JsonNode`, or `SqlHierarchyId`) to and from a database column. You implement `Parse` and `SetValue` methods and register it globally via `SqlMapper.AddTypeHandler`.
26. **Explain how Dapper interacts with the ADO.NET Connection Pool.**
    *Answer:* Dapper itself does not manage connection pooling; it relies entirely on the underlying ADO.NET `SqlConnection`. When you call `connection.QueryAsync`, if the connection is closed, Dapper will implicitly call `OpenAsync()`, execute the query, and close it, returning the connection to the ADO.NET pool.
27. **What happens if you use `IEnumerable<T>` with `buffered: false` (Unbuffered) and try to iterate it twice?**
    *Answer:* You will get an exception or unexpected behavior because the underlying `SqlDataReader` is forward-only. Once it has been read and closed, it cannot be iterated again. Unbuffered results must be processed exactly once.
28. **How does Dapper handle multiple result sets (MARS) vs GridReader?**
    *Answer:* MARS (Multiple Active Result Sets) is a SQL Server feature that allows multiple pending requests on a single connection. Dapper's `QueryMultiple` (GridReader) does not require MARS; it executes a single batch of SQL containing multiple statements and sequentially advances the reader using `NextResult()`.
29. **Why is using `Task.Run` to wrap synchronous Dapper calls an anti-pattern in ASP.NET Core?**
    *Answer:* It causes "sync-over-async" thread pool starvation. It ties up a thread pool thread just to block and wait for database I/O. Instead, you must use Dapper's native `*Async` methods which utilize non-blocking IO via the underlying `SqlCommand.ExecuteDbDataReaderAsync`.
30. **Describe the performance implications of column order in your SQL SELECT statement when using Dapper.**
    *Answer:* Because Dapper generates IL based on the specific columns returned by the reader, if you change the column order (e.g., `SELECT Id, Name` vs `SELECT Name, Id`), Dapper treats this as a different cache miss and generates a new IL delegate. While minor, consistency in column selection helps optimize the cache.

### Staff Engineer (5)
31. **Design a bulk insert strategy for 500,000 records. Is Dapper `Execute` with an array of parameters the right choice?**
    *Answer:* No. Passing an IEnumerable to Dapper's `Execute` generates an individual parameterized INSERT statement for every item and executes them sequentially (or in small batches), resulting in massive network latency. For 500,000 records, you should completely bypass Dapper and use `SqlBulkCopy`, or pass the data as a User-Defined Table Type (TVP) via Dapper to a stored procedure.
32. **Explain how you would implement Row-Level Security (RLS) in a Multi-Tenant SaaS application using Dapper without appending `WHERE TenantId = @id` to every query.**
    *Answer:* You use SQL Server native Row-Level Security policies. Before executing any Dapper query for a request, you execute a preliminary Dapper command to set the SQL Server session context (e.g., `EXEC sp_set_session_context 'TenantId', @TenantId;`). SQL Server then automatically applies the security predicate to all subsequent queries executed on that specific `SqlConnection`.
33. **We are seeing high CPU usage on the SQL Server and the DBA reports "Ad-hoc query plan cache bloat". The application strictly uses Dapper. What is the root cause?**
    *Answer:* The application is likely generating dynamic SQL strings with hardcoded values instead of parameters (e.g., `WHERE Status = 'Active'` vs `WHERE Status = 'Inactive'`). SQL Server treats each distinct string as a new query and compiles a new execution plan, bloating the plan cache and wasting CPU. We must audit the codebase to ensure `DynamicParameters` or anonymous objects are used strictly for all variables.
34. **How do you architect a resilient database retry policy (e.g., for transient Azure SQL errors) around Dapper?**
    *Answer:* Dapper does not have built-in retry logic. You must use a resilience library like **Polly**. You wrap the Dapper invocation in a Polly `AsyncRetryPolicy` configured to handle specific `SqlException` error numbers (like 40613 for database unavailable or 10928 for resource limits) and apply exponential backoff.
35. **Your Dapper queries are suddenly taking 3x longer after a .NET version upgrade, but SQL Server execution times are unchanged. How do you profile and identify the bottleneck?**
    *Answer:* The issue is likely in the application layer mapping or network payload. I would use tools like **dotnet-trace** and **PerfView** to analyze the CPU time. I would look specifically for high allocations in `System.Reflection.Emit` or GC pauses. If the Dapper internal cache was somehow being busted repeatedly, we would see high CPU in IL compilation. I would also check if a new `TypeHandler` was introduced that performs synchronous blocking I/O or heavy serialization (e.g., JSON parsing).

### Architect (5)
36. **Architect a CQRS Event-Sourced system. Where does Dapper fit, and how do you handle eventual consistency between the Write model and Read model?**
    *Answer:* In an Event-Sourced system, the Write model appends events to an Event Store (e.g., EventStoreDB or Cosmos DB). A background worker (e.g., a hosted service or Azure Function) subscribes to this event stream. This worker uses Dapper to execute ultra-fast `UPSERT` statements to update the Read Models (Projectors) stored in SQL Server. The Read API uses Dapper to query these pre-calculated projections. Eventual consistency is handled by returning an ETag or Version Number from the Command API, allowing the client to poll the Read API or receive a SignalR notification when the projection is updated.
37. **Evaluate the architectural trade-offs of using Dapper vs a Source-Generated ORM (like EF Core 8+ compiled models or RepoDB) for a new ultra-low-latency microservice.**
    *Answer:* Dapper relies on runtime IL Emit. While incredibly fast, it requires the JIT compiler to run upon the first execution of a query, causing a slight latency spike (cold start). Source-Generated ORMs generate the mapping C# code at compile-time, eliminating JIT overhead entirely and making them perfectly suited for Native AOT (Ahead-of-Time) compilation. If the microservice must run in an extreme low-latency environment (e.g., high-frequency trading) or serverless environment with strict cold-start requirements, a Source-Generated ORM might edge out Dapper. However, Dapper's maturity, ecosystem, and flexibility with raw T-SQL often outweigh the microsecond cold-start penalty for standard enterprise APIs.
38. **How would you implement a distributed caching strategy (Redis) on top of Dapper repositories to handle 100,000 reads/second for static tenant configuration?**
    *Answer:* I would implement the Decorator pattern. I would define an interface `ITenantConfigRepository`. The primary implementation (`TenantConfigDapperRepository`) queries the database using Dapper. The decorator (`TenantConfigRedisDecorator`) injects both the Dapper repository and an `IDistributedCache`. The decorator first checks Redis. On a cache miss, it calls the Dapper repository, serializes the result via `System.Text.Json`, stores it in Redis with a TTL, and returns it. This keeps caching logic completely separated from Dapper logic.
39. **In a geographically distributed database architecture (e.g., Azure SQL Geo-Replication), how do you configure Dapper to leverage read-only replicas safely?**
    *Answer:* At the infrastructure level, I would maintain two connection strings: a Read/Write string and a Read-Only string (using `ApplicationIntent=ReadOnly`). At the application layer, I would use Dependency Injection to provide an `IWriteDbConnectionFactory` and an `IReadDbConnectionFactory`. CQRS Command handlers inject the Write factory. Query handlers inject the Read factory. Dapper executes blindly against whatever `IDbConnection` the factory provides, ensuring queries hit the secondary replica without application-level logic changes.
40. **Justify the decision to migrate a large monolithic EF Core application's reporting endpoints to Dapper to a non-technical stakeholder.**
    *Answer:* "Our current reporting system uses a framework (EF Core) designed for ensuring data integrity when *saving* data, but we are using it to *read* massive amounts of data. It's like using an armored truck to deliver a single pizza—it works, but it's slow, expensive, and consumes excessive resources (memory/CPU). By migrating just the reporting endpoints to Dapper, we swap the armored truck for a high-speed motorcycle. This will significantly reduce report load times from minutes to seconds, improve the user experience, and lower our Azure hosting costs by reducing server memory requirements, all without rewriting the core business logic of the application."

## 9. Exercises

### Easy
1.  **Environment Setup:** Create a new .NET Console Application. Install the `Dapper` and `Microsoft.Data.SqlClient` NuGet packages.
2.  **Hello World:** Write a Dapper query that executes `SELECT 'Hello Dapper' AS Message` and maps the result to a `string`. Output it to the console.

### Medium
1.  **Parameterized Query:** Create a `Tenant` class with `Id` (Guid) and `Name` (string) properties. Create a SQL Server LocalDB database and a `Tenants` table. Write a Dapper query to insert a tenant, and another query to select that tenant by `Id` using parameterized inputs.
2.  **Null Handling:** Add a `Nullable<DateTime>` column `DeactivatedAt` to the `Tenants` table. Write a query to retrieve it and ensure your C# object correctly handles the `null` value when the tenant is active.

### Hard
1.  **CQRS Simulation:** Create an `ICommand` interface and an `IQuery` interface. Implement a Command that uses an EF Core `DbContext` to add a new `Site` to a `Tenant`. Implement a Query that uses Dapper to retrieve all `Sites` for a `Tenant`. Ensure both read from the same database.
2.  **Unbuffered Processing:** Insert 100,000 dummy records into a local SQL table. Write an unbuffered Dapper query (`buffered: false`). As you iterate over the results, calculate a running total of a specific column, proving you can process the data without storing all 100,000 objects in memory simultaneously.

### Enterprise
1.  **Polyglot Repository:** Implement a complete Vertical Slice for our EV Charging Platform (e.g., "Create Charger" and "Get Charger Details"). The write model must enforce domain invariants (a charger cannot be created if the Site is inactive) using a standard Repository or EF Core. The read model must use Dapper to execute a complex SQL View that joins `Chargers`, `Sites`, and `Tenants`, returning a flattened `ChargerDetailDto`.

## 10. Summary

Dapper was born in the fires of extreme scalability at Stack Overflow. It is not an Object-Relational Mapper in the traditional sense; it is a high-performance object mapper for ADO.NET. By leveraging runtime IL Emit, Dapper achieves mapping speeds indistinguishable from hand-written ADO.NET while completely eliminating the boilerplate.

For the Enterprise Architect building SaaS platforms, Dapper represents the perfect tool for the Query side of a CQRS architecture. It provides absolute control over T-SQL execution, stateless memory efficiency essential for multi-tenant applications, and seamless integration with the .NET ecosystem. However, mastering Dapper requires accepting responsibility: you are no longer shielded by LINQ; you must become an expert in T-SQL and database performance. In the next chapter, we will dissect Dapper's internal architecture to understand exactly how it achieves its remarkable performance.
